# 관리자 대시보드 전용
Django>=5.0,<6.0
daphne>=4.1          # ASGI 서버. MJPEG 스트리밍에 필수
redis>=5.0           # 없으면 /tmp 파일 폴백으로 동작하지만 권장하지 않음
whitenoise>=6.6        # static 파일 서빙
Pillow>=10.0         # ImageField 처리
numpy
paho-mqtt>=2.1,<3.0   # jetson-multicam-re_id-tracking 의 cctv/entry 토픽 구독용 (mqtt_worker.py)
opencv-python>=4.9   # 로컬 카메라 캡처/인코딩용 (tracker_worker.py). 실제 검출/추적/Re-ID
                     # 모델을 붙일 때는 그 모델이 요구하는 추가 패키지(torch 등)도 필요하다.
