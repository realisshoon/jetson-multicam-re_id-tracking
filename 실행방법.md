# 실행방법

본 문서는 최종 제출용 실행 가이드입니다. 전체 시스템은 **Jetson Camera A/B/C/D + MQTT Broker + Main Server + Django Web Dashboard**로 구성됩니다.

## 1. 사전 준비

- Python 3.10
- Jetson Orin Nano 4대: JetPack / CUDA / TensorRT / OpenCV / PyTorch가 보드에 설치되어 있어야 합니다.
- Main Server PC: Eclipse Mosquitto 설치
- 각 장치는 동일 네트워크에 연결합니다.
- MQTT Broker 주소가 바뀌면 `configs/mqtt.yaml` 및 실행 환경의 서버 주소를 실제 Main PC IP로 수정합니다.

## 2. 모델 파일 배치

Git에는 대용량 모델 바이너리를 포함하지 않습니다. 최종 제출 ZIP에는 아래 파일을 함께 넣고 지정 경로에 배치합니다.

- `yolo26n.pt`
- `models/reid/person_reid_osnet_x0_25_fp16.engine`
- `models/face/face_detection_yunet_2023mar.onnx` (Camera A)
- `models/face/face_recognition_sface_2021dec.onnx` (Camera A)

정확한 파일 크기와 SHA-256은 `models/MANIFEST.md`를 확인합니다.

## 3. 가상환경 및 requirements 설치

### Camera A
```bash
python3 -m venv .venv --system-site-packages
source .venv/bin/activate
pip install -r requirements/camera-a.txt
```

### Camera B
```bash
python3 -m venv .venv --system-site-packages
source .venv/bin/activate
pip install -r requirements/camera-b.txt
```

### Camera C
```bash
python3 -m venv .venv --system-site-packages
source .venv/bin/activate
pip install -r requirements/camera-c.txt
```

### Camera D
```bash
python3 -m venv .venv --system-site-packages
source .venv/bin/activate
pip install -r requirements/camera-d.txt
```

### Main Server
```powershell
py -3.10 -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements/main-server.txt
```

### Web Dashboard
```powershell
py -3.10 -m venv .venv-web
.\.venv-web\Scripts\Activate.ps1
pip install -r requirements/web.txt
```

> 루트의 `requirements.txt`는 Jetson 공통 Python 패키지 확인용이며, 실제 실행 시에는 위와 같이 **역할별 requirements 파일**을 사용하는 것을 권장합니다.

## 4. 실행 순서

아래 순서로 실행합니다.

### 1) Main PC - MQTT Broker
```powershell
mosquitto -c configs/mosquitto.main-server.conf
```

### 2) Main PC - Main Server
```powershell
.\.venv\Scripts\Activate.ps1
python -m cctv_main.main_server
```

기본 포트:
- MQTT Broker: `1883`
- Main REST API: `8080`
- Main Admin API: `8091`

### 3) Jetson D
```bash
source .venv/bin/activate
./scripts/run_node_d.sh
```

### 4) Jetson B / C
```bash
# Camera B
source .venv/bin/activate
./scripts/run_node_b.sh

# Camera C
source .venv/bin/activate
./scripts/run_node_c.sh
```

### 5) Jetson A
```bash
source .venv/bin/activate
./scripts/run_node_a.sh
```

### 6) Web Dashboard
```powershell
.\.venv-web\Scripts\Activate.ps1
cd web
python manage.py migrate

daphne -b 0.0.0.0 -p 8000 config.asgi:application
```

필요 시 별도 터미널에서 Main Server 연동 worker를 실행합니다.

```powershell
cd web
python main_server_worker.py
```

## 5. 보드별 실행 파일

| 역할 | 실행 파일 | requirements | 기본 웹 포트 |
|---|---|---|---:|
| Camera A | `src/nodes/node_a.py` / `scripts/run_node_a.sh` | `requirements/camera-a.txt` | 8000 |
| Camera B | `src/nodes/node_b.py` / `scripts/run_node_b.sh` | `requirements/camera-b.txt` | 8001 |
| Camera C | `src/nodes/node_c.py` / `scripts/run_node_c.sh` | `requirements/camera-c.txt` | 8002 |
| Camera D | `src/nodes/node_d.py` / `scripts/run_node_d.sh` | `requirements/camera-d.txt` | 8003 |
| Main Server | `cctv_main/main_server.py` | `requirements/main-server.txt` | 8080 / 8091 |
| Web | `web/` | `requirements/web.txt` | 8000 |

## 6. 정상 동작 확인

1. Camera A에서 사람이 감지되면 ENTRY 이벤트가 Main Server에 등록되는지 확인합니다.
2. B 또는 C에서 동일 인물 PASSAGE가 확인되는지 확인합니다.
3. D에서 ARRIVAL이 확인되는지 확인합니다.
4. 재방문 시 기존 Person UID가 유지되고 새로운 Journey가 생성되는지 확인합니다.
5. 관리자 페이지에서 A/B/C/D 영상과 Journey/Person/Event 정보가 표시되는지 확인합니다.

상세 시스템 설명과 네트워크/MQTT 계약은 `README.md`, `docs/PORTS.md`, `docs/MQTT_CONTRACT.md`를 참고합니다.
